export interface ITheme {
  colors: {
    primary: string;
    secondary: string;
    tertiary: string;
    background: string;
    darkText: string;
    lightText: string;
  };
  fontSizes: {
    sm: string;
    md: string;
    lg: string;
  };
}
